"""Configuration management for compatibility evaluation system.

This package provides clean separation between default configuration values,
environment-based overrides, and runtime settings. Configuration is immutable
once loaded and provides type safety for all compatibility evaluation parameters.
"""
